```js
    async function enesgkky(){
        const oneDay = 24;
        const startDay = setInterval(()=>{
            wakeability();
            await code();
            toMuchSleep()
                .then(() => ?iAmDead() && clearInterval(startDay))
        },oneDay)
    }    
```

